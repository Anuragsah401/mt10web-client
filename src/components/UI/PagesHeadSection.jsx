import React from 'react'

const PagesHeadSection = ({title}) => {
  return (
    <div>
        <h1 className="text-center text-3xl font-bold text-gray-900">
            {title}
        </h1>
    </div>
  )
}

export default PagesHeadSection